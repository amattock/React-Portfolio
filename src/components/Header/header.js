import React from 'react';
import Navbar from '../Navbar/navLinks'; // Assuming the Navbar component is in the same directory

const Header = () => {
  return (
    <header>
      <h1>Welcome to My Portfolio</h1>
      <Navbar />
    </header>
  );
};

export default Header;
